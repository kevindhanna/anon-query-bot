source 'https://rubygems.org'

gem 'slack-ruby-client'
gem 'puma'
gem 'sinatra'
gem 'redis'
gem 'celluloid-io'

group :development, :test do
    gem 'dotenv'
    gem 'rake'
end

group :test do
end
